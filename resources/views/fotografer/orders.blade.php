<x-fg-layout><div class="space-y-6"><div class="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><h1>Pesanan & Transaksi</h1><p class="helper mt-2">Semua transaksi yang terhubung ke karya Anda.</p></div><a class="button rounded-lg bg-black px-5 py-3 text-white" href="{{ route('fotografer.orders.export', request()->query()) }}">Export CSV</a></div><form class="flex gap-2" method="GET"><input class="w-full max-w-md rounded-lg border-gray-300 text-sm" name="q" value="{{ request('q') }}" placeholder="Cari order, pembeli, foto, atau event"><button class="rounded-lg border border-gray-300 bg-white px-4">Cari</button></form><div class="overflow-hidden rounded-2xl border bg-white"><div class="overflow-x-auto"><table class="w-full"><thead class="bg-gray-50"><tr><th class="p-4 text-left">Order</th><th class="p-4 text-left">Foto & Event</th><th class="p-4 text-left">Pembeli</th><th class="p-4 text-right">Harga</th><th class="p-4 text-right">Pendapatan 90%</th><th class="p-4 text-left">Status</th><th class="p-4 text-right">Waktu</th></tr></thead><tbody class="divide-y">@forelse($transactions as $trx)<tr><td class="p-4 font-bold">{{ $trx->order_number ?? '#'.$trx->id }}</td><td class="p-4">{{ $trx->photo?->title }}<span class="block text-xs text-gray-500">{{ $trx->photo?->event?->nama_event }}</span></td><td class="p-4">{{ $trx->pembeli?->name }}<span class="block text-xs text-gray-500">{{ $trx->pembeli?->email }}</span></td><td class="p-4 text-right">Rp{{ number_format($trx->harga_foto) }}</td><td class="p-4 text-right font-bold">Rp{{ number_format($trx->jumlah_fotografer) }}</td><td class="p-4 uppercase">{{ $trx->payment_status }}</td><td class="p-4 text-right">{{ ($trx->paid_at ?? $trx->created_at)->format('d M Y H:i') }}</td></tr>@empty<tr><td colspan="7" class="p-10 text-center text-gray-500">Tidak ada transaksi.</td></tr>@endforelse</tbody></table></div><div class="p-4">{{ $transactions->links() }}</div></div></div></x-fg-layout>
