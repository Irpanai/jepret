<x-superadmin-layout><div class="space-y-6"><div class="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><h1>Transaction Ledger</h1><p class="helper mt-2">Snapshot finansial 90/10 untuk seluruh transaksi.</p></div><a class="button rounded-lg bg-black px-5 py-3 text-white" href="{{ route('superadmin.orders.export',request()->query()) }}">Export CSV</a></div><form class="grid gap-3 rounded-xl border bg-white p-4 sm:grid-cols-4" method="GET"><input class="rounded-lg border-gray-300 text-sm sm:col-span-2" name="q" value="{{ request('q') }}" placeholder="Order, buyer, fotografer, foto, event"><input class="rounded-lg border-gray-300 text-sm" type="date" name="from" value="{{ request('from') }}"><input class="rounded-lg border-gray-300 text-sm" type="date" name="to" value="{{ request('to') }}"><button class="rounded-lg bg-black px-4 py-2 text-white">Terapkan filter</button></form><div class="grid gap-4 sm:grid-cols-2 xl:grid-cols-5">@foreach([['Transaksi',$metrics['count']],['Paid',$metrics['paid_count']],['GMV','Rp'.number_format($metrics['gmv'])],['Fotografer','Rp'.number_format($metrics['photographer'])],['Platform','Rp'.number_format($metrics['platform'])]] as $card)<div class="rounded-xl border bg-white p-4"><p class="text-xs uppercase text-gray-500">{{ $card[0] }}</p><p class="metric mt-2">{{ $card[1] }}</p></div>@endforeach</div><div class="overflow-hidden rounded-2xl border bg-white"><div class="overflow-x-auto"><table class="w-full"><thead class="bg-gray-50"><tr>@foreach(['ID / Waktu','Buyer','Fotografer / Event','GMV','90%','10%','Fee','Status'] as $head)<th class="p-4 text-left">{{ $head }}</th>@endforeach</tr></thead><tbody class="divide-y">@forelse($transactions as $trx)<tr><td class="p-4"><strong>{{ $trx->order_number ?? '#'.$trx->id }}</strong><small class="block text-gray-500">{{ $trx->created_at->format('d M Y H:i') }}</small></td><td class="p-4">{{ $trx->pembeli?->name }}<small class="block text-gray-500">{{ $trx->pembeli?->email }}</small></td><td class="p-4">{{ $trx->fotografer?->name ?? $trx->photo?->fotografer?->name }}<small class="block text-gray-500">{{ $trx->photo?->event?->nama_event }}</small></td><td class="p-4">Rp{{ number_format($trx->total_bayar) }}</td><td class="p-4 font-bold">Rp{{ number_format($trx->jumlah_fotografer) }}</td><td class="p-4">Rp{{ number_format($trx->jumlah_platform) }}</td><td class="p-4">{{ $trx->payment_fee_amount===null?'Not recorded':'Rp'.number_format($trx->payment_fee_amount) }}</td><td class="p-4 uppercase">{{ $trx->payment_status }}</td></tr>@empty<tr><td colspan="8" class="p-10 text-center">Tidak ada data.</td></tr>@endforelse</tbody></table></div><div class="p-4">{{ $transactions->links() }}</div></div></div></x-superadmin-layout>
